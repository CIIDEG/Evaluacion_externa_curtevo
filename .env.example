# Dominio donde se publica el portal (debe apuntar al VPS)
DOMAIN=cutervo.metacalidad.cloud
ADMIN_EMAIL=Mayamida66@gmail.com

# Credenciales del panel de administración
ADMIN_USER=admin
ADMIN_PASS=cambia-esto-por-una-clave-fuerte

# Llave secreta de sesión (genera con: python -c "import secrets;print(secrets.token_hex(32))")
SECRET_KEY=cambia-esto-por-una-clave-aleatoria-larga

# Ruta de la BD SQLite (se monta en volumen)
DATABASE_URL=sqlite:////code/data/cutervo.db
